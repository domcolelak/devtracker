# Screenshots

Placeholders for the screenshots referenced from the main README. To reproduce
them, boot the stack (see the Quickstart section of the main README), then:

- `django-admin.png` - open http://localhost/admin/ and log in as `admin` with
  the seed password, then capture the Teams changelist with its membership
  inline.
- `swagger-ui.png` - open http://localhost/api/docs and capture the expanded
  POST /tasks operation.
