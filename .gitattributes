# NORMALIZE LINE ENDINGS: STORE LF IN THE REPOSITORY FOR ALL TEXT FILES
* text=auto

# FILES EXECUTED INSIDE LINUX CONTAINERS MUST CHECK OUT WITH LF EVEN ON WINDOWS,
# OTHERWISE /bin/sh FAILS ON CARRIAGE RETURNS
*.sh text eol=lf
Dockerfile text eol=lf
*.Dockerfile text eol=lf
